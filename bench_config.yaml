# how many logs to insert before measuring
pre_seed_logs: 100000

# phase durations (seconds)
steady_state_secs: 120       # baseline measurement without writes
heavy_write_secs: 180       # duration of concurrent write load
recovery_secs: 180           # post-write recovery measurement

# paths
logstorm_config: "./logstorm_config.yaml"
qstorm_configs_dir: "./qstorm_configs"
results_dir: "./results"

# qstorm backends to test (run simultaneously)
backends:
  qdrant:
    config: "qdrant.yaml"
    queries: "queries.yaml"
  elasticsearch:
    config: "elastic.yaml"
    queries: "queries.yaml"
  # pgvector:
  #   config: "pgvector.yaml"
  #   queries: "queries.yaml"
  opensearch:
    config: "opensearch.yaml"
    queries: "queries.yaml"